<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h2 class="font-semibold text-xl text-gray-800 dark:text-gray-100">Affectations pédagogiques</h2><a href="{{ route('pedagogical-configuration.index', ['school_year_id' => $schoolYear?->id, 'tab' => 'assignments']) }}" class="text-sm text-indigo-600 dark:text-indigo-400">Retour à la configuration</a></div></x-slot>
    <div class="py-8"><div class="max-w-7xl mx-auto sm:px-6 lg:px-8 space-y-6">
        @if($schoolYear)<div class="rounded-lg bg-white p-6 shadow-sm dark:bg-slate-800"><h3 class="font-semibold text-gray-900 dark:text-white">Assistant d’affectation — {{ $schoolYear->year_string }}</h3><p class="mt-1 text-sm text-gray-500 dark:text-gray-400">Choisissez un professeur, une ou plusieurs classes, puis les matières à lui confier.</p><form method="POST" action="{{ route('pedagogical-configuration.assignments.store') }}" class="mt-5 space-y-5">@csrf @if($errors->any())<div class="rounded-md border border-red-200 dark:border-red-900 bg-red-50 dark:bg-red-900/20 p-4 text-sm text-red-800 dark:text-red-200"><p class="font-semibold">L’affectation n’a pas été enregistrée.</p><ul class="mt-2 list-disc space-y-1 pl-5">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<input type="hidden" name="school_year_id" value="{{ $schoolYear->id }}"><div class="rounded-md border border-gray-200 p-4 dark:border-slate-700"><h4 class="text-sm font-semibold text-gray-900 dark:text-white">Informations générales</h4><div class="mt-3 space-y-4"><div><label class="block text-sm font-medium dark:text-gray-200">Professeur (nom — matricule)</label><select name="teacher_matricule" class="mt-1 w-full rounded-md border-gray-300 dark:border-slate-600 dark:bg-slate-900 dark:text-white" required><option value="">Sélectionner un professeur</option>@foreach($teachers as $teacher)<option value="{{ $teacher->matricule }}" @selected(old('teacher_matricule') == $teacher->matricule)>{{ $teacher->user?->name ?? '—' }} — {{ $teacher->matricule }}</option>@endforeach</select>@error('teacher_matricule')<p class="mt-1 text-xs text-red-600">{{ $message }}</p>@enderror</div><div><p class="text-sm font-medium dark:text-gray-200">Classe(s) concernée(s)</p>@error('classroom_ids')<p class="mt-1 text-xs text-red-600">{{ $message }}</p>@enderror<div class="mt-2 grid grid-cols-1 gap-2 sm:grid-cols-2 md:grid-cols-3">@foreach($classrooms as $classroom)<label class="flex items-center gap-2 rounded border-gray-200 dark:border-slate-600 border p-3 text-sm dark:text-gray-200"><input type="checkbox" name="classroom_ids[]" value="{{ $classroom->id }}"><span class="flex-1">{{ $classroom->name }} <span class="text-gray-500 dark:text-gray-400">{{ $classroom->cycle }}</span></span><input type="number" name="classroom_volumes[{{ $classroom->id }}]" value="{{ old('classroom_volumes.'.$classroom->id, 0) }}" min="0" max="50" step="0.5" class="w-20 rounded border-gray-300 dark:border-slate-600 dark:bg-slate-900 dark:text-white text-center" aria-label="Volume hebdomadaire pour {{ $classroom->name }}"><span class="text-xs text-gray-500 dark:text-gray-400">h/sem.</span></label>@endforeach</div></div></div></div><div><p class="text-sm font-medium dark:text-gray-200">Matière(s)</p>@error('new_subject_names')<p class="mt-1 text-xs text-red-600">{{ $message }}</p>@enderror<div class="mt-2 grid grid-cols-1 gap-2 sm:grid-cols-2 md:grid-cols-3">@foreach($matieres as $matiere)<label class="flex items-center gap-2 rounded border-gray-200 dark:border-slate-600 border p-3 text-sm dark:text-gray-200"><input type="checkbox" name="matiere_ids[]" value="{{ $matiere->id }}">{{ $matiere->nom }}</label>@endforeach</div></div><div><label class="block text-sm font-medium dark:text-gray-200">Nouvelle(s) matière(s)</label><input name="new_subject_names" value="{{ old('new_subject_names') }}" placeholder="Ex. Arabe, Français, Mathématiques, SVT" class="mt-1 w-full rounded-md border-gray-300 dark:border-slate-600 dark:bg-slate-900 dark:text-white"><p class="mt-1 text-xs text-gray-500 dark:text-gray-400">Séparez plusieurs matières par des virgules. Elles seront créées si elles n’existent pas.</p></div><button class="rounded-md bg-indigo-600 px-5 py-2 text-sm font-semibold text-white">Confirmer les affectations</button></form></div>
        <form method="GET" class="grid grid-cols-1 gap-3 rounded-lg bg-white p-4 shadow-sm md:grid-cols-5 dark:bg-slate-800"><input type="hidden" name="school_year_id" value="{{ $schoolYear->id }}"><select name="teacher_id" class="rounded-md border-gray-300 dark:border-slate-600 dark:bg-slate-900 dark:text-white"><option value="">Tous les professeurs</option>@foreach($teachers as $teacher)<option value="{{ $teacher->id }}" @selected(request('teacher_id') == $teacher->id)>{{ $teacher->user?->name ?? '—' }}</option>@endforeach</select><select name="classroom_id" class="rounded-md border-gray-300 dark:border-slate-600 dark:bg-slate-900 dark:text-white"><option value="">Toutes les classes</option>@foreach($classrooms as $classroom)<option value="{{ $classroom->id }}" @selected(request('classroom_id') == $classroom->id)>{{ $classroom->name }}</option>@endforeach</select><select name="matiere_id" class="rounded-md border-gray-300 dark:border-slate-600 dark:bg-slate-900 dark:text-white"><option value="">Toutes les matières</option>@foreach($matieres as $matiere)<option value="{{ $matiere->id }}" @selected(request('matiere_id') == $matiere->id)>{{ $matiere->nom }}</option>@endforeach</select><button class="rounded-md bg-slate-700 px-4 py-2 text-sm font-semibold text-white">Filtrer</button></form>
        <div class="overflow-hidden rounded-lg bg-white shadow-sm dark:bg-slate-800"><div class="border-b p-5 dark:border-slate-700"><h3 class="font-semibold dark:text-white">Affectations enregistrées</h3></div><div class="overflow-x-auto"><table class="min-w-full divide-y"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500 dark:bg-slate-700 dark:text-gray-400"><tr><th class="px-5 py-3">Professeur</th><th class="px-5 py-3">Classe</th><th class="px-5 py-3">Matière</th><th class="px-5 py-3">Cycle</th><th class="px-5 py-3">Volume / semaine</th><th class="px-5 py-3">Statut</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y dark:divide-slate-700">@forelse($assignments as $assignment)<tr class="text-sm dark:text-gray-200"><td class="px-5 py-4">{{ $assignment->teacher->user?->name ?? '—' }}</td><td class="px-5 py-4">{{ $assignment->classroom->name }}</td><td class="px-5 py-4">{{ $assignment->matiere->nom }}</td><td class="px-5 py-4">{{ $assignment->classroom->cycle }}</td><td class="px-5 py-4">{{ $assignment->volume_horaire_hebdo }} h</td><td class="px-5 py-4">{{ $assignment->is_active ? 'Actif' : 'Désactivé' }}</td><td class="px-5 py-4"><form method="POST" action="{{ route('pedagogical-configuration.assignments.toggle', $assignment) }}">@csrf @method('PATCH')<button class="text-indigo-600">{{ $assignment->is_active ? 'Désactiver' : 'Activer' }}</button></form></td></tr>@empty<tr><td colspan="7" class="px-5 py-10 text-center text-sm text-gray-500 dark:text-gray-400">Aucune affectation.</td></tr>@endforelse</tbody></table></div><div class="p-5">{{ $assignments->links() }}</div></div>
        @else<div class="rounded bg-amber-50 dark:bg-amber-900/20 p-5 text-amber-800 dark:text-amber-200">Aucune année scolaire active.</div>@endif
    </div></div>
</x-app-layout>
